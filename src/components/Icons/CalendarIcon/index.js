import { Icon } from '../Icon';
import { calendar } from '../iconsPaths/calendar';

export const CalendarIcon = () => {
  return (
    <Icon color={'gray-600'} size={'20'}>
      {calendar}
    </Icon>
  );
};
